# Tasks — web UI prototype (graham-bell)

1. Extend `Status` with optional `on_line` capture — done
2. FastAPI app + SSE `/api/run` — done
3. Static elegant UI with scrolling log — done
4. DEMO.md + sample logs — done
5. Gherkin web scenarios (Validated) — done
6. Beads for production rebuild — done
